<div class="panel panel-default">
    <?php if(!$edit): ?>
        <div class="panel-heading">
            <div class="col-sm-12 col-md-8">
                <h4>Recordings</h4>
            </div>
            <div class="col-sm-12 col-md-4 text-right">
                
            </div>
        </div>
    <?php endif; ?>
    <div class="panel-body">
        <?php if($edit): ?>
            <button id="add-to-event-venue-button" class="btn btn-xs btn-primary" data-toggle="modal" data-target="#add-to-event-venue-modal">
                <i class="fa fa-link"></i>&nbsp;Add
                recording
            </button>
            <br/><br/>
            <?php if(count($video->recordings) > 0): ?>
                <ul class="list-group">
                    <?php foreach($video->recordings as $recording): ?>
                        <?php if($recording->pricing): ?>
                        <li class="list-group-item" style="margin-bottom: 10px">
                            <?php echo e($recording->pricing->event->name); ?>

                            <br>
                            <small><?php echo e(ucfirst($recording->pricing->name)); ?></small>
                            <div class="btn-group pull-right" role="group">
                                <a href="<?php echo e(route('admin.recordings.destroy',$recording->id)); ?>"
                                   class="btn btn-default btn-xs">
                                    <i class="fa fa-trash"></i>
                                </a>
                            </div>
                        </li>
                        <?php endif; ?>
                    <?php endforeach; ?>
                </ul>
            <?php else: ?>
                <?php if($edit): ?>
                    <p>This video is not in any recordings.</p>
                <?php endif; ?>
            <?php endif; ?>
        <?php else: ?>
            <p>First create the video to add recordings.</p>
        <?php endif; ?>
    </div>
</div>